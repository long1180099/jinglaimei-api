FROM nginx:alpine

# 移除默认配置
RUN rm /etc/nginx/conf.d/default.conf

# 复制自定义Nginx配置（API反向代理到云托管内部后端）
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 复制React构建产物
COPY build/ /usr/share/nginx/html/

# 暴露80端口
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
